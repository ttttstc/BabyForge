# 鸿蒙原生内测版不排队离线写入照护事实

鸿蒙原生内测版允许读取最近成功同步的内容并保留本机可恢复草稿，但不会把照护事实加入后台写入队列。恢复网络后，照护者必须重新确认提交；对响应结果不明的请求先查询服务端结果，而不是直接重试创建。这个选择牺牲了完全离线记录的便利，换取更简单、可解释的事实一致性，避免重复事件、隐性冲突和“看似成功但尚未入库”的高风险状态；完整离线同步应作为拥有独立协议、冲突策略与验收边界的后续能力。
