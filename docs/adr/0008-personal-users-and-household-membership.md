# ADR-0008：个人用户与家庭成员关系取代共享账号

- 状态：已采纳
- 日期：2026-08-10
- 取代：ADR-0001

BabyForge 使用独立个人用户作为稳定认证身份，并以家庭成员关系决定用户可以访问哪个家庭的宝宝数据。邮箱、用户名和 Google 只作为同一用户的登录标识；Owner 与 Member 是家庭内角色，记录人仍只表示事实录入来源。该模型取代家庭共享登录凭据，使成员撤权、家庭邀请和跨家庭数据隔离能够在服务端明确执行，同时不把记录人误用为权限主体。
