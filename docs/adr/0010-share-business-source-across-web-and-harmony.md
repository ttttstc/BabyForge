# Web 与鸿蒙客户端共享业务源而不共享界面

ArkWeb 研究原型之后，BabyForge 将 Web 与鸿蒙原生客户端作为并存的两个表达层：它们共享账号、家庭、事实协议、确定性规则、已发布内容、媒体资产和服务接口，但各自实现符合平台习惯的界面。这样既避免复制会漂移的业务与医学安全逻辑，也避免为了复用 React 页面而牺牲鸿蒙原生操作；复杂 3D 等能力是否采用受控混合承载，由后续页面边界单独决定。
